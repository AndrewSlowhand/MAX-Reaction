# MAX Reaction

Автоматизация реакций и действий в чат-ботах MAX через браузер.

Проект предназначен для автоматизации работы с ботами MAX, когда у разных организаций используются разные адреса ботов, но принцип взаимодействия одинаковый.

> **Важно:** проект не содержит адрес конкретного бота. Перед первым запуском пользователь должен самостоятельно указать URL нужного MAX-бота.

## Возможности

- автоматический запуск MAX через встроенный Chromium;
- автоматический переход через кнопку **«Открыть в браузере»**, если она появляется;
- установка реакции 👍, если она доступна;
- нажатие **«Полезно»**, если кнопка есть;
- нажатие 🔥, если реакция доступна в посте;
- сохранение состояния уже обработанных сообщений;
- поддержка нескольких профилей MAX;
- ежедневный запуск через Планировщик заданий Windows;
- скрытый запуск без окна CMD;
- portable-сборка: на конечном компьютере Python устанавливать не требуется.

## Как это работает

```text
Запуск
   ↓
Chromium
   ↓
MAX-бот
   ↓
«Открыть в браузере» (если появляется)
   ↓
Профиль MAX
   ↓
Новые посты
   ↓
👍 / Полезно / 🔥
```

## 1. Указать своего бота

Откройте файл:

```text
bot_url.txt
```

и укажите адрес нужного бота, например:

```text
https://max.ru/YOUR_BOT
```

**Не используйте в публичном репозитории адрес конкретного учреждения без необходимости.**

У разных организаций могут быть разные MAX-боты. Менять код для этого не требуется — достаточно изменить URL.

## 2. Первый запуск

Для первоначальной авторизации запустите:

```text
login.bat
```

Откроется Chromium.

Если MAX покажет кнопку **«Открыть в браузере»**, программа нажмёт её автоматически.

После этого выполните вход в нужный профиль MAX.

## 3. Ежедневный запуск

Для обычной проверки:

```text
daily.bat
```

Для запуска без окна командной строки:

```text
daily_hidden.vbs
```

Последний вариант удобно использовать в Планировщике заданий Windows.

## Реакции

Режим:

```text
both
```

обрабатывает доступные действия:

- 👍;
- **Полезно**;
- 🔥.

Если конкретной кнопки в посте нет, она пропускается.

## Профили

Можно использовать несколько профилей MAX.

Например:

```bat
login.bat "https://max.ru/example_bot" account1
```

и:

```bat
login.bat "https://max.ru/example_bot" account2
```

Данные профилей сохраняются отдельно.

## Сборка

Для сборки проекта на компьютере разработчика используется:

```text
build.bat
```

После сборки можно создать portable-архив:

```text
package_portable.bat
```

На компьютере конечного пользователя Python устанавливать не нужно.

В portable-пакет входит необходимый Chromium.

## Планировщик заданий Windows

Рекомендуемый вариант — запускать:

```text
wscript.exe
```

с аргументом:

```text
daily_hidden.vbs
```

Это позволяет выполнять автоматизацию без видимого окна CMD.

## Структура проекта

```text
MAX-Reaction/
├── README.md
├── README_RU.md
├── LICENSE
├── .gitignore
├── bot_url.txt
├── reaction.py
├── build.bat
├── login.bat
├── daily.bat
├── daily_hidden.vbs
├── package_portable.bat
└── VERSION.txt
```

## Требования

### Для компьютера разработчика

- Windows;
- Python;
- интернет для установки зависимостей и Chromium;
- PyInstaller.

### Для готовой portable-сборки

- Windows;
- Python не требуется;
- отдельная установка Chromium не требуется.

## Безопасность

Не публикуйте в GitHub:

- данные авторизации;
- cookies;
- папки профилей;
- содержимое `data/`;
- личные конфигурации;
- URL ботов, которые вы не хотите публиковать.

## Лицензия

Проект распространяется на условиях лицензии, указанной в `LICENSE`.

---

Если проект оказался полезен, можно поставить ⭐ репозиторию.
